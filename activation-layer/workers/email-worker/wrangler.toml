name = "hotstack-email"
main = "src/index.js"
compatibility_date = "2024-09-01"

# Set with `wrangler secret put SENDGRID_API_KEY` before deploying.
[vars]
FROM_EMAIL = "no-reply@faa.zone"
FROM_NAME = "HotStack"
