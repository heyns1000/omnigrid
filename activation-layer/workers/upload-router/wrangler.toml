name = "quantum-nexus"
main = "src/index.js"
compatibility_date = "2024-09-01"

[[r2_buckets]]
binding = "INTAKE_BUCKET"
bucket_name = "hotstack-intake-bucket"

[vars]
# Comma-separated list of downstream worker URLs this router can dispatch to.
ORCHESTRATOR_URL = "https://hotstack-orchestrator.<account>.workers.dev/intake"
